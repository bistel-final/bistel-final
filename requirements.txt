# =====================================================================
#  Python 패키지 목록  (Python 3.11 또는 3.12 권장)
#
#  설치 : python -m venv .venv
#         .venv\Scripts\activate          (Windows)
#         source .venv/bin/activate       (WSL / macOS)
#         pip install -r requirements.txt
#
#  ⚠️ Python 3.13 은 일부 ML 패키지 휠이 아직 없을 수 있다. 3.12 를 권장한다.
# =====================================================================

# ---------------------------------------------------------------------
# 데이터 처리 · 이상감지  (팀원 1)
# ---------------------------------------------------------------------
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
scipy==1.14.1
joblib==1.4.2                 # 학습 모델 저장·로드
matplotlib==3.9.2             # 지표 시각화

# ---------------------------------------------------------------------
# DB 접속
# ---------------------------------------------------------------------
psycopg[binary]==3.2.3        # PostgreSQL 드라이버 (psycopg3)
SQLAlchemy==2.0.36            # pandas.read_sql 연동
pgvector==0.3.6               # vector 타입 파이썬 바인딩
neo4j==5.26.0                 # Neo4j Bolt 드라이버

# ---------------------------------------------------------------------
# LangGraph 에이전트 · Tool  (팀원 3 · 핵심)
# ---------------------------------------------------------------------
langgraph==0.2.53
langgraph-checkpoint-postgres==2.0.9   # 체크포인트를 PostgreSQL 에 저장 → HITL 재개
langchain-core==0.3.21

# LLM 제공자 — 사용할 것만 남기고 나머지는 주석 처리해도 된다
langchain-ollama==0.2.1       # 로컬 Ollama (무료, rate limit 없음)
# langchain-google-genai==2.0.6  # Gemini 무료 티어
# langchain-groq==0.2.1          # Groq 무료 티어
# langchain-openai==0.2.10       # OpenAI 호환 엔드포인트
# langchain-anthropic==0.3.0     # Claude

# ---------------------------------------------------------------------
# RAG · 임베딩  (팀원 2)
# ---------------------------------------------------------------------
sentence-transformers==3.3.1  # bge-m3 등 로컬 임베딩 실행
langchain-text-splitters==0.3.2  # 문서 청킹
python-frontmatter==1.1.0     # md 파일 YAML 헤더 파싱
tiktoken==0.8.0               # 토큰 수 계산(청크 크기 조절용)

# ---------------------------------------------------------------------
# Text2SQL · UI · 평가  (팀원 4)
# ---------------------------------------------------------------------
streamlit==1.40.2
plotly==5.24.1                # 대시보드 차트
streamlit-agraph==0.0.45      # 관계 그래프 시각화
sqlglot==26.0.1               # 생성된 SQL 검증·파싱 (쓰기 구문 차단에 사용)

# ---------------------------------------------------------------------
# 공통 유틸
# ---------------------------------------------------------------------
python-dotenv==1.0.1          # .env 로드
pydantic==2.10.3              # Tool 입출력 스키마 정의·검증
tenacity==9.0.0               # Tool 재시도 정책
httpx==0.28.1
tqdm==4.67.1

# ---------------------------------------------------------------------
# 테스트 · 코드 품질
# ---------------------------------------------------------------------
pytest==8.3.4
pytest-cov==6.0.0
ruff==0.8.3                   # 린터 + 포매터 (black·flake8 대체)

# ---------------------------------------------------------------------
# FastAPI API 계층 — React 전환에 따라 추가
# ---------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1